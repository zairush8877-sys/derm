<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Aura" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/aura_bg</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/aura_bg</item>
    </style>
    <!-- Сплэш-экран при запуске -->
    <style name="Theme.Aura.Splash" parent="Theme.Aura">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>
